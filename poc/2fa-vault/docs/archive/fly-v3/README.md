# ARCHIVE — unused Fly v3 experiment

This directory is the retired Fly.io attempt (`arkade-vault-demo-api`).
That app no longer exists. It used inverted CSV 288/4032, a RecoveryKey
environment variable, and env-injected key material. Do not deploy it.

The live Mutinynet authorizer is Railway `authorizer-next` built from
`Dockerfile.mutinynet`.
